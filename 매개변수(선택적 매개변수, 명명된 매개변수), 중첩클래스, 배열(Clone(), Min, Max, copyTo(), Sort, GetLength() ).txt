매개변수(선택적 매개변수, 명명된 매개변수), 중첩클래스, 배열(Clone(), Min, Max, copyTo(), Sort, GetLength() )

선택적 매개변수 : 매개변수 선언 부분에 값을 주는것 : 필수매개변수가 반드시 선택 매개변수 앞에 와야한다
명명된 파라미터 : 파라미터에 목적지를 정해줌

중첩클래스 : 중첩클래스를 많이 쓰지 않지만 쓰는 이유는 딱하나
	    한 메서드에 있는 변수를 다른 메서드에서 쓰고싶을때 전역변수로는 쓰기싫으면 중첩클래스 사용해
	    지역변수를 두고 같이 사용
배열: 배열은 레퍼런스타입

인터페이스: 클래스와 다르게 디폴트가 퍼블릭
	   추상클래스보다 인터페이스를 더 많이 쓰고 규모를 조금 작게 작게 만들어야함
	   볼륨이 작은경우는 인터페이스 큰경우는 추상클래스 
	   클래스와 다르게 다중상속 가능!
	   인터페이스가 인터페이스 상속도 가능